// Single source of truth for brand identity constants.
// The default logo (client/public/logo.jpg) is the guaranteed fallback — the
// whole branding system falls back to it when no admin logo is configured or
// when the branding API is unavailable.
export const BRAND_NAME = '24x7Chhutti'
export const BRAND_LOGO_FALLBACK = '/logo.jpg'