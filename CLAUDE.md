# CLAUDE.md

Conciencia for Claude Code working on **24x7Chhutti**.

**Read `AGENTS.md` first.** It is the primary, authoritative project instruction
source and overrides generic coding habits. Follow it strictly.

- Read the relevant docs in `docs/` before making architectural changes.
- Follow the locked **MERN architecture** (React + Vite frontend, Express REST
  backend, MongoDB + Mongoose). Never switch to Next.js, PostgreSQL, Prisma,
  Firebase, Supabase, or GraphQL without approval.
- Read `docs/CURRENT_STATE.md` before continuing development and update it after
  meaningful milestones.
- Never delete, rename, replace, or modify `logo.jpg` — it is the official
  project logo and must be preserved exactly.
- Keep changes small and focused; never rewrite working code unnecessarily.
- Never commit secrets or `.env` files.

For full details, see `AGENTS.md` and the documentation in `docs/`.
